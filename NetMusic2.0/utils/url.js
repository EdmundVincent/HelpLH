
var ip ="localhost:3000"
module.exports= {
  // 排行榜
  topList: `http://${ip}/top/list`,
  playList: `http://${ip}/song/detail`,
  lyric: `http://${ip}/lyric`,
  search: `http://${ip}/search`,
  hot: `http://${ip}/search/hot`
}